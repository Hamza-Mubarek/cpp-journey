version 1.0
    -project initiated
version 1.1
    -added menu, so the user can choose operation
    -added switch case for the menu
    -made the formating better
version 1.2
    -the UX got better significantly
    -added conditions for a faulty input for the menu
    -clean exit got possible
version 1.3
    -I used function as they should
     -I made them the house for logic and decision making only or each with one function while
     -I made the main function for displaying and performing some UI
     -I added a dicision making process using bool in the division function